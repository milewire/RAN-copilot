[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "cp -r ../engine . || cp -r ../../engine . || true",
  "cp -r ../ai . || cp -r ../../ai . || true"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"

