"""
Analyzer facade package for RAN-Copilot.

These modules primarily re-export implementations from the existing
`engine` package so we don't rebuild the backend from scratch while
still matching the desired folder layout.
"""


